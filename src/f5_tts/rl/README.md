# RL resources

- Place ASR model weights under `SenseVoiceSmall/` (or set `F5TTS_RL_ASR_MODEL` to a Hugging Face repo id or local path).
- Place speaker embedding weights under `wespeaker/chinese/` (or set `F5TTS_RL_SPK_MODEL`).
- Paths are resolved relative to this folder by default.
